#include<stdio.h>
#include<assert.h>
char* my_stycpy(char* dest, const char* src)
{
	assert(dest != NULL);
	assert(src != NULL);
	char *p = dest;
	while (*dest++ = *src++);
	return p;
}
int main()
{
	char a[10] = { 0 };
	char *q = "hello";
	printf("%s",my_stycpy(a,q));

	return 0;
}
